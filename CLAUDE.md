# CLAUDE.md

This file gives AI assistants (Claude Code and similar tools) the context needed to work in this repository.

## Current State

This repository (`linh-eng/AI-Agent`) is **empty**: no commits, no source files, no branches other than the one currently in progress. There is no existing codebase structure, build system, dependency manifest, or convention to document yet.

If you are reading this because you were asked to make changes here, do not assume the presence of any framework, language, or directory layout — check first (`ls -la`, `git log`, `git branch -a`) rather than relying on this file's memory of a prior state.

## Working in This Repo

Until real code lands, there are no established workflows to follow. As soon as the first meaningful commit is made (e.g. a package manifest, an app scaffold, a language toolchain config), this file should be updated to describe:

- **Project purpose** — what the AI-Agent project is meant to do.
- **Structure** — top-level directories and what lives in each.
- **Build / run / test commands** — the actual commands (e.g. `npm install`, `npm test`, `pip install -r requirements.txt`, etc.) once a toolchain exists.
- **Conventions** — code style, naming patterns, testing approach, and any architectural decisions specific to this project.
- **Development workflow** — branching model, how PRs are structured, CI checks that must pass.

## Maintaining This File

Whoever (human or AI) adds the first real code to this repository should replace this placeholder with an accurate description of what was built, generated from the actual files present — not from assumptions. Keep this file in sync with the codebase as it evolves; stale documentation is worse than no documentation.
